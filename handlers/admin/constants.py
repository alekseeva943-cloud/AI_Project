"""
handlers/admin/constants.py

Центральное хранилище констант административной панели.

Что это за файл:
Содержит все "магические" числа, состояния для пошаговых сценариев (ConversationHandler)
и настройки интерфейса по умолчанию. Вынесено сюда, чтобы не захламлять бизнес-логику
и чтобы значения можно было менять в одном месте.

Логика блоков:
- Состояния сгруппированы по модулям (Пользователи, Рассылка, База знаний).
- Номера состояний идут с шагом (1, 10, 20, 30, 1000+), чтобы в будущем 
  можно было легко вставить промежуточные шаги без сдвига остальных.
"""

# ==========================================================
# СОСТОЯНИЯ ConversationHandler: ПОЛЬЗОВАТЕЛИ И ЧАТЫ
# Используются в handlers/admin/users.py и handlers/admin/client_messages.py
# ==========================================================

# Ожидание ввода поискового запроса (имя, телефон, username) 
# в сценарии поиска клиента.
AWAITING_SEARCH_QUERY = 1

# Ожидание ввода текста сообщения, которое админ хочет отправить 
# конкретному клиенту из его карточки.
AWAITING_MESSAGE_TO_CLIENT = 20


# ==========================================================
# СОСТОЯНИЯ ConversationHandler: МАССОВАЯ РАССЫЛКА
# Используются в handlers/admin/broadcast.py
# ==========================================================

# Ожидание выбора типа аудитории рассылки (Всем / Только с телефоном).
AWAITING_BROADCAST_TYPE = 10

# Ожидание ввода самого текста рассылки.
AWAITING_BROADCAST_TEXT = 11

# Ожидание финального подтверждения отправки (Да / Нет).
AWAITING_BROADCAST_CONFIRM = 12


# ==========================================================
# СОСТОЯНИЯ ConversationHandler: БАЗА ЗНАНИЙ (RAG)
# Используются в handlers/admin/knowledge_base.py
# ==========================================================

# Ожидание ввода нового URL-адреса сайта для парсинга базы знаний.
AWAITING_RAG_SOURCE_URL = 30

# Ожидание ввода нового лимита количества страниц для парсинга.
AWAITING_CRAWL_LIMIT = 1002

# Ожидание подтверждения запуска долгого процесса сборки новой базы.
AWAITING_BUILD_CONFIRMATION = 1003

# Ожидание подтверждения переключения бота на новую (только что собранную) базу знаний.
AWAITING_ACTIVATE_CONFIRMATION = 1004

# Ожидание подтверждения отката на предыдущую резервную копию базы знаний.
AWAITING_RESTORE_CONFIRMATION = 1005


# ==========================================================
# НАСТРОЙКИ ПО УМОЛЧАНИЮ: БАЗА ЗНАНИЙ (RAG)
# Используются в handlers/admin/knowledge_base.py и database.py
# ==========================================================

# Стандартное количество страниц, которое парсер попытается скачать за один раз,
# если админ не указал иное значение.
DEFAULT_CRAWL_LIMIT = 4

# URL сайта, который будет использоваться как источник данных для RAG,
# если в настройках базы не указан другой адрес.
DEFAULT_RAG_SOURCE = "https://professional24.ru"


# ==========================================================
# НАСТРОЙКИ ИНТЕРФЕЙСА: ПАГИНАЦИЯ
# Используются в handlers/admin/users.py для постраничного вывода списков
# ==========================================================

# Максимальное количество карточек пользователей, 
# которое бот отправляет в чат за один раз (на одной странице).
PAGE_SIZE = 10