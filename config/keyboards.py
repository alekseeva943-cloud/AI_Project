# keyboards.py

#

# Путь:

# project/config/keyboards.py

#

# Назначение:

# Централизованное хранение всех клавиатур Telegram-бота.

#

# Зачем нужен этот файл:

# - все кнопки находятся в одном месте;

# - изменение кнопки производится один раз;

# - исчезает дублирование кода в handlers;

# - новые разделы добавляются значительно быстрее;

# - статистика может использовать те же кнопки, что и интерфейс.

#

# Используется в:

# - handlers/utilities.py

# - handlers/admin.py

# - handlers/start.py

# - handlers/gpt_chat.py

# - других обработчиках проекта

from telegram import KeyboardButton, ReplyKeyboardMarkup

# ==========================================================

# ОБЩИЕ КНОПКИ

# Используются практически во всём проекте.

# ==========================================================

# Кнопка возврата в предыдущее меню.

BACK_BUTTON = KeyboardButton("⬅️ Вернуться")

# Кнопка отмены текущего действия.

CANCEL_BUTTON = KeyboardButton("❌ Отмена")

# Кнопка отправки номера телефона.

# Telegram автоматически предлагает пользователю отправить контакт.

PHONE_BUTTON = KeyboardButton(
"📞 Оставить телефон",
request_contact=True
)

# Кнопка отправки текущего местоположения.

# Telegram автоматически предлагает пользователю отправить геолокацию.

LOCATION_BUTTON = KeyboardButton(
"📍 Отправить локацию",
request_location=True
)

# ==========================================================

# СПИСОК ОБРАЩЕНИЙ ЗА ПОМОЩЬЮ

# Используется одновременно:

#

# - в клавиатуре помощи;

# - в статистике обращений;

# - в аналитике администратора;

# - в будущем AI-маршрутизаторе.

# ==========================================================

HELP_REQUESTS = [
"🛞 Спустило колесо",
"⛽ Нет топлива",
"🚗 Не заводится",
"❄️ Отогреть",
"🌬 Кондиционер",
"⚡ Электрика",
"🔓 Вскрыть",
"💻 Диагностика",
"❓ Прочее"
]

# ==========================================================

# ОСНОВНЫЕ УСЛУГИ КОМПАНИИ

# Главное меню услуг.

# ==========================================================

SERVICE_BUTTONS = [
["🛞 Шиномонтаж", "❄️ Кондиционер"],
["🚗 Хранение", "🔧 Авто-помощь"]
]

# ==========================================================

# УСЛУГИ РАЗДЕЛА "АВТО-ПОМОЩЬ"

# Подменю экстренной помощи на дороге.

# ==========================================================

AUTO_HELP_BUTTONS = [
["⛽ Подвоз топлива", "💡 Запуск двигателя"],
["🔒 Отключение сигнализации", "💻 Компьютерная диагностика"]
]

# ==========================================================

# КЛАВИАТУРА ПОМОЩИ НА ДОРОГЕ

#

# Используется:

# handlers.utilities.show_help_menu()

# ==========================================================

def get_help_keyboard():
    return ReplyKeyboardMarkup(
    [
    [KeyboardButton("🛞 Спустило колесо"),
    KeyboardButton("⛽ Нет топлива")],


            [KeyboardButton("🚗 Не заводится"),
            KeyboardButton("❄️ Отогреть")],

            [KeyboardButton("🌬 Кондиционер"),
            KeyboardButton("⚡ Электрика")],

            [KeyboardButton("🔓 Вскрыть"),
            KeyboardButton("💻 Диагностика")],

            [KeyboardButton("❓ Прочее")],

            [BACK_BUTTON]
        ],
        resize_keyboard=True
    )


# ==========================================================

# КЛАВИАТУРА ОСНОВНЫХ УСЛУГ

#

# Используется:

# handlers.utilities.handle_services()

# ==========================================================

def get_services_keyboard():
    return ReplyKeyboardMarkup(
    [
    [KeyboardButton("🛞 Шиномонтаж"),
    KeyboardButton("❄️ Кондиционер")],


            [KeyboardButton("🚗 Хранение"),
            KeyboardButton("🔧 Авто-помощь")],

            [BACK_BUTTON]
        ],
        resize_keyboard=True
    )


# ==========================================================

# КЛАВИАТУРА АВТО-ПОМОЩИ

#

# Используется:

# handlers.utilities.show_auto_help_submenu()

# ==========================================================

def get_auto_help_keyboard():
    return ReplyKeyboardMarkup(
    [
    [KeyboardButton("⛽ Подвоз топлива"),
    KeyboardButton("💡 Запуск двигателя")],


            [KeyboardButton("🔒 Отключение сигнализации"),
            KeyboardButton("💻 Компьютерная диагностика")],

            [BACK_BUTTON]
        ],
        resize_keyboard=True
    )


# ==========================================================

# КЛАВИАТУРА СВЯЗИ С КЛИЕНТОМ

#

# Используется после выбора проблемы пользователем.

#

# Позже сюда можно добавить:

# - отправку фотографии;

# - отправку видео;

# - отправку VIN автомобиля;

# - отправку госномера.

# ==========================================================

def get_contact_keyboard():
    return ReplyKeyboardMarkup(
    [
    [PHONE_BUTTON],
    [LOCATION_BUTTON],
    [BACK_BUTTON]
    ],
    resize_keyboard=True
    )

# ==========================================================

# КЛАВИАТУРА ОТМЕНЫ ДЕЙСТВИЯ

#

# Используется в ConversationHandler:

# - поиск клиентов;

# - рассылка;

# - написание сообщения клиенту;

# - будущие сценарии диалогов.

# ==========================================================

def get_cancel_keyboard():
    return ReplyKeyboardMarkup(
    [
    [CANCEL_BUTTON]
    ],
    resize_keyboard=True
    )

# ==========================================================

# КЛАВИАТУРА ПОДТВЕРЖДЕНИЯ ДЕЙСТВИЯ

#

# Используется:

# - подтверждение рассылки;

# - подтверждение удаления;

# - другие опасные действия.

# ==========================================================

def get_confirm_keyboard():
    return ReplyKeyboardMarkup(
    [
    [KeyboardButton("✅ Подтвердить")],
    [CANCEL_BUTTON]
    ],
    resize_keyboard=True
    )


'''
==========================================================
КЛАВИАТУРА ВОЗВРАТА
Используется для экранов, где нужна только одна кнопка:
- описание услуг;
- контакты компании;
- информационные сообщения;
- GPT-ответы;
- будущие справочные разделы.
Преимущество:
если когда-нибудь потребуется изменить кнопку
"⬅️ Вернуться" на что-то другое,
изменение потребуется сделать только здесь.

==========================================================
'''

def get_back_keyboard():
    return ReplyKeyboardMarkup(
    [
    [BACK_BUTTON]
    ],
    resize_keyboard=True
    )


#===========================
def get_main_keyboard(is_admin_user: bool = False):
    keyboard = [
        [KeyboardButton("🆘 Нужна помощь")],
        [
            KeyboardButton("📞 Оставить телефон", request_contact=True),
            KeyboardButton("📍 Отправить локацию", request_location=True)
        ],
        [
            KeyboardButton("🛠 Наши услуги"),
            KeyboardButton("📱 Наши контакты")
        ]
    ]

    if is_admin_user:
        keyboard.insert(0, [KeyboardButton("🛠 Админка")])

    return ReplyKeyboardMarkup(
        keyboard=keyboard,
        resize_keyboard=True
    )